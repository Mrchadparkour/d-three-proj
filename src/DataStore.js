import { action, extendObservable, computed } from 'mobx';
// import MainObj from './capacity_and_booking_lead_time.json';

export class DataStore {
  constructor() {
    extendObservable(this, {

    })
  }
}

export default new DataStore();
